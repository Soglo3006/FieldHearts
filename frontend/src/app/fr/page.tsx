import HomePage from "@/app/page";

// French coming soon entry point — renders the same homepage (visible as background)
// The ComingSoonOverlay in layout detects /fr and shows French content
export default HomePage;
